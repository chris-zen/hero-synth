[package]
name = "hero_host"
version = "0.1.0"
authors = ["Christian Perez-Llamas"]

[dependencies]
portaudio = "0.7.0"
docopt = "0.7.0"
rand = "0.3.15"

# coremidi = "0.1.0"

hero_core = { path = "../core" }
hero_synth = { path = "../synth" }
